#include<stdio.h>
#include<vector>
int botton_up_cut(vector<int> & precio, n ){

}
int main(){
    vector<int> numeros;numeros[0]=1;numeros[1]=5;
    numeros[2]=8;numeros[3]=9;numeros[4]=10;
    numeros[5]=17;numeros[6]=17;numeros[7]=20;
    botton_up_cut(precio,)
}
